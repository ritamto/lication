{
  "name": "lication",
  "private": true,
  "scripts": {
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && npm start",
    "deploy": "npm run build && cd backend && npm install && cd .. && npx wrangler deploy"
  }
}